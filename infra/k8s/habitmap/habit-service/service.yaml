apiVersion: v1
kind: Service
metadata:
  name: habit-service
  namespace: habitmap
  labels:
    app: habit-service
spec:
  type: ClusterIP
  selector:
    app: habit-service
  ports:
    - name: http
      port: 3002
      targetPort: 3002
      protocol: TCP
