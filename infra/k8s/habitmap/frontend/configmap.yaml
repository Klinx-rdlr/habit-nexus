apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: habitmap
data:
  PORT: "4000"
  NODE_ENV: "production"
  # NEXT_PUBLIC_* vars are baked into client-side JS at build time via Docker build arg
  NEXT_PUBLIC_API_URL: "https://api.project1.rhodereyes.dev"
