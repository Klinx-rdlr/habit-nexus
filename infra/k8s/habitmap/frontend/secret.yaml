apiVersion: v1
kind: Secret
metadata:
  name: frontend-secret
  namespace: habitmap
type: Opaque
data:
  # No secrets needed for frontend currently
  # Add any server-side secrets here if needed in the future
  PLACEHOLDER: "bm9uZQ=="   # none
