apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: habitmap
  labels:
    app: frontend
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - name: http
      port: 4000
      targetPort: 4000
      protocol: TCP
